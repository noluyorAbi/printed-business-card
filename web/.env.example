# The geometry worker. Both are server only: no NEXT_PUBLIC_ prefix, ever.
# Copy this to .env.local and fill it in. .env.local is gitignored.
WORKER_URL=your-worker-host-here
WORKER_TOKEN=your-token-here
